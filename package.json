{
  "name": "reviewer-approve-required-action",
  "version": "0.1.0",
  "description": "",
  "main": "build/main.js",
  "scripts": {
    "start:local": "act",
    "test": "jest --collect-coverage",
    "test:watch": "jest --watchAll",
    "build": "tsc -p tsconfig.json && ncc build build/main.js",
    "lint": "eslint --ext .js --ext .tsx --fix --max-warnings 0",
    "prettier": "prettier src",
    "prettier:fix": "npm run prettier -- --write 'src/**/*.ts'",
    "all": "npm run build && npm run prettier:fix && npm run lint && npm run build && npm test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "eslint": "^8.49.0",
    "prettier": "^3.0.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
